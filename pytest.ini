[pytest]
testpaths = hxi_optimizer/tests
asyncio_mode = auto
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    safety_critical: marks tests that verify safety-critical behaviour
